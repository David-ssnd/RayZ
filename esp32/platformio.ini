; ============================================================================
; ROOT platformio.ini - MINIMAL STUB (points to subprojects)
; ============================================================================
; This stub exists only to satisfy PlatformIO extension detection.
; Actual builds use subproject configurations to avoid CMake errors.
;
; ACTIVE CONFIGURATIONS:
;   - weapon/platformio.ini (use: cd weapon; pio run -t upload)
;   - target/platformio.ini  (use: cd target; pio run -t upload)
; ============================================================================

[platformio]
default_envs = weapon

[env:weapon]
platform = espressif32@6.12.0
board = esp32-c3-devkitm-1
framework = espidf

[env:target]
platform = espressif32@6.12.0
board = esp32dev
framework = espidf
